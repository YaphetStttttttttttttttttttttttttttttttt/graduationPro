<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
    <mapper namespace="com.gp.mapper.UserMapper">
        <select id="slogin" resultType="int">
            select count(*) from s_user where username = #{username} and password = #{password}
        </select>
        <select id="tlogin" resultType="int">
            select count(*) from t_user where username = #{username} and password = #{password}
        </select>
        <select id="alogin" resultType="int">
            select count(*) from admin where username = #{username} and password = #{password}
        </select>
    </mapper>